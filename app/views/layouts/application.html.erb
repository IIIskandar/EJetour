<!DOCTYPE html>
<html>
  <head>
    <title>Eje Tour</title>
    <link href="https://fonts.googleapis.com/css?family=Lobster" rel="stylesheet">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<header>
  		<nav class="be-green be-blue large-padding">
  			<ul type="none" class="no list row center-xs middle-xs">
  				<li >
  					<h1 class="no-margin" id="logo" >Eje Tour</h1>
  				</li>
  				<li class="col-md">
  					<%= link_to "Inicio", root_path%>
  				</li>
  				<li class="col-md">
  					<% unless current_user %>
  						<%= link_to "Iniciar Sesion Con Facebook", user_facebook_omniauth_authorize_path %>
					<% else %>
  						<%= link_to "Logout", destroy_user_session_path, method: :delete %>
					<% end %>
  				</li>
  				<li class="col-md">
  					<% if current_user %>
						<div><%=current_user.name%>  <%= image_tag(current_user.image)%></div>
					<% end %>
  				</li>
  			</ul>
  			
  		</nav>
  	</header>
  	
    <%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
